# Changelog

## TODO

- [ ] **Dev container templates** — add `services/devcontainers/` with base configs for common stacks (CUDA/PyTorch, ROS2, plain Python); handle multi-arch (amd64/arm64) via build args
